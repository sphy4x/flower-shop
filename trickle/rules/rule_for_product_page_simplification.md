When user asks to remove a product page section or option everywhere
- Remove the UI block from the product page and any other pages that display it
- Remove related state, price logic, and cart payload fields so the feature cannot reappear
- Ensure any conditional rendering is valid JSX and does not duplicate wrappers